@model IEnumerable<HelpDeskLogin.Models.Arquivos>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.DH_Cadastro)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DH_Cadastro)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.idArquivo">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.idArquivo">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.idArquivo">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
