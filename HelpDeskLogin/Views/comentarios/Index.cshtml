@model IEnumerable<HelpDeskLogin.Models.comentarios>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.comentario)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.DHComentario)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.comentario)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DHComentario)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.idComentario">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.idComentario">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.idComentario">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
